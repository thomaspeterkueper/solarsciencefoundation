'use client';
import { useState } from 'react';
export default function SwellingExperiment(){const [moisture,setMoisture]=useState(20);const [sealed,setSealed]=useState(false);const response=Math.round(moisture*(sealed?.35:1));return <div style={{display:'grid',gap:12}}><strong>Feuchteaufnahme und Quellung</strong><label>Feuchteangebot: {moisture}%<input type="range" min="0" max="100" value={moisture} onChange={e=>setMoisture(+e.target.value)} style={{width:'100%'}}/></label><label><input type="checkbox" checked={sealed} onChange={e=>setSealed(e.target.checked)}/> vereinfachte Schutzschicht</label><div>Qualitative Quellneigung: <strong>{response}%</strong></div><p style={{margin:0}}>Holz ist ein hygroskopischer, zellulärer Werkstoff. Wasseraufnahme kann Dimensionsänderungen verursachen. Das Modell illustriert den Zusammenhang nur qualitativ; Holzart, Faserrichtung, Beschichtung und Ausgangsfeuchte bestimmen reale Werte.</p></div>}
