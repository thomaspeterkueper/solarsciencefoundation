'use client';
import { useState } from 'react';
export default function WiperTechniqueExperiment(){const [angle,setAngle]=useState(45);const [speed,setSpeed]=useState(50);return <div style={{display:'grid',gap:12}}><strong>Abziehen: Geometrie und Bewegung vergleichen</strong><label>Wischerwinkel: {angle}°<input type="range" min="20" max="90" value={angle} onChange={e=>setAngle(+e.target.value)} style={{width:'100%'}}/></label><label>relative Geschwindigkeit: {speed}<input type="range" min="10" max="100" value={speed} onChange={e=>setSpeed(+e.target.value)} style={{width:'100%'}}/></label><div>Beobachte als Modellfrage: <strong>Wie verändern Winkel und Geschwindigkeit die Flüssigkeitsführung an der Wischkante?</strong></div><p style={{margin:0}}>Es wird bewusst kein universeller „optimaler Winkel“ behauptet. Ergebnis und Handhabung hängen unter anderem von Wischergeometrie, Gummilippe, Flüssigkeitsfilm, Oberfläche und Bewegung ab.</p></div>}
