'use client';
import { useState } from 'react';
export default function PlantTurgorExperiment(){const [outside,setOutside]=useState(30);const gradient=50-outside;const state=gradient>10?'Wasseraufnahme begünstigt':gradient<-10?'Wasserabgabe begünstigt':'geringer osmotischer Antrieb';return <div style={{display:'grid',gap:12}}><strong>Pflanzenzelle: Wasserpotential qualitativ erkunden</strong><label>Relative Außenkonzentration gelöster Stoffe: {outside}<input type="range" min="0" max="100" value={outside} onChange={e=>setOutside(+e.target.value)} style={{width:'100%'}}/></label><div>Zustand im vereinfachten Modell: <strong>{state}</strong></div><p style={{margin:0}}>Turgor entsteht nicht durch Taupunktphysik. Wasserbewegung über Membranen hängt vom Wasserpotential ab; gelöste Stoffe sind nur ein Teil davon. Der Slider ist bewusst qualitativ und keine Berechnung realer Zellpotentiale.</p></div>}
