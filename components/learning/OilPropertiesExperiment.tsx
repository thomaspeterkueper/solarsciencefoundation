'use client';
import { useState } from 'react';
export default function OilPropertiesExperiment(){const [temp,setTemp]=useState(20);const [water,setWater]=useState(false);const viscosity=Math.max(15,Math.round(100-(temp-10)*1.2));return <div style={{display:'grid',gap:12}}><strong>Öl: Fließverhalten statt Verbrennungsmodell</strong><label>Temperatur: {temp} °C<input type="range" min="10" max="70" value={temp} onChange={e=>setTemp(+e.target.value)} style={{width:'100%'}}/></label><label><input type="checkbox" checked={water} onChange={e=>setWater(e.target.checked)}/> Wasserphase hinzufügen</label><div>Qualitative relative Viskosität: <strong>{viscosity}</strong></div><p style={{margin:0}}>{water?'Öl und Wasser bilden ohne geeignete Emulgierung getrennte Phasen. ':'Öl zeigt temperaturabhängiges Fließverhalten. '}Der Zahlenwert ist eine didaktische Skala, keine Stoffdatenbank. Reale Viskosität hängt stark von der konkreten Ölzusammensetzung ab.</p></div>}
